#####################
## Used to define load data parameters, they change based on dataset.
#####################
label_index = [0];
one_hot_depth = 2;
keys_index = [1];
#record_defaults = ["", [float(0)],]; # Default values, in case of empty columns. Also specifies the type of the decoded result.
#record_defaults.extend([[float(0)] for j in range(300)])
#print(record_defaults);
#print(len(record_defaults));
#exit();